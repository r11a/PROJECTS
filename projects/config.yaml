name: "PROJECTS"
version: "0.13.0"
slug: "projects"
description: "Premium smart-home project management dashboard"
url: "https://github.com/r11a/PROJECTS"
arch:
  - amd64
startup: application
boot: auto
init: false
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:view-dashboard-variant
panel_title: PROJECTS
panel_admin: true
ports:
  8686/tcp: 8686
ports_description:
  8686/tcp: "PROJECTS standalone web interface (editable host port)"
map:
  - type: share
    read_only: false
  - type: media
    read_only: false
options:
  admin_username: "admin"
  admin_password: "change-me-now"
schema:
  admin_username: str
  admin_password: password
webui: "http://[HOST]:[PORT:8686]"
watchdog: "http://[HOST]:[PORT:8686]/health"
backup: cold
stage: experimental
